# Backtest Report

Not enough data yet: 1 draw(s) loaded, but a walk-forward backtest needs more than 35 draws to produce a meaningful result (it needs `--min-history` warm-up draws before it can even start predicting, plus enough draws after that to say anything statistically). This section will populate automatically as more real draws are collected.
